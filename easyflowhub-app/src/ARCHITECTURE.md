# Deprecated Architecture Note

本文件保留仅为兼容旧链接，不再作为 EasyFlowHub 的权威架构文档。

原因：

- 内容仍使用旧的 `Deskflow` 命名
- 技术栈、目录结构和发展规划已经与当前仓库不一致
- 大段结构抄录易继续腐化，已不适合作为 AI 与维护者的事实来源

当前权威入口请改为读取：

1. `../../docs/PROJECT_PROFILE.md`
2. `../../docs/roadmap.md`
3. `../../docs/ARCHITECTURE_GUARDRAILS.md`
4. 相关目录下的 `MODULE.md`
5. 根目录 `../../AGENTS.md`

如需恢复具体设计，请优先在 `docs/plans` 或 `docs/decisions` 中沉淀当前版本信息，而不是重新扩写本文件。
