# <YYYY-MM-DD> <plan-title>

> status: draft
> owner: <team-or-person>
> related_modules: `<module-a>, <module-b>`
> related_roadmap_item: `<roadmap bullet>`
> supersedes:

## 1. Problem

- Current problem: `<what is wrong now>`
- User / system impact: `<impact>`
- Why now: `<why this needs attention>`

## 2. Goals

- `<goal>`
- `<goal>`

## 3. Non-Goals

- `<not in scope>`
- `<not in scope>`

## 4. Current State

- `<current behavior>`
- `<current architecture note>`
- `<constraints already in code>`

## 5. Proposed Change

- `<proposed change>`
- `<proposed change>`
- `<proposed change>`

## 6. Boundary Check

- Existing implementation searched: `<paths / symbols / commands>`
- Canonical owner after change: `<module / package>`
- New duplication introduced: `<no / yes + explain>`
- Architecture doc to sync: `<doc path>`

## 7. Risks

- `<risk>`
- `<risk>`

## 8. Verification Plan

- `<build / typecheck>`
- `<tests>`
- `<manual or E2E path>`

## 9. Exit Criteria

- `<condition>`
- `<condition>`
- `<condition>`

## 10. Follow-Up

- `<follow-up task>`
- `<follow-up task>`

## 11. Resolution

Fill after implementation:

- Outcome: `<implemented / abandoned / superseded>`
- Verified by: `<commands / tests / manual checks>`
- Docs updated: `<paths>`
