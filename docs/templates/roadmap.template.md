# <PROJECT_NAME> Roadmap

## <CURRENT_VERSION>

### Current Goal

- `<current release objective>`
- `<current release objective>`
- `<current release objective>`

### Active Tracks

#### Product / Feature

- `<active work item>`
- `<active work item>`

#### Architecture / Quality

- `<active work item>`
- `<active work item>`

#### AI Workflow / Tooling

- `<active work item>`
- `<active work item>`

### Recent Progress

- `<YYYY-MM-DD> <completed or materially advanced item>`
- `<YYYY-MM-DD> <completed or materially advanced item>`
- `<YYYY-MM-DD> <completed or materially advanced item>`

### Validation Debt

- `<what still needs manual or automated verification>`
- `<what still needs manual or automated verification>`

### Next Queue

- `<next priority>`
- `<next priority>`
- `<next priority>`

## Candidate Next Version

### <NEXT_VERSION>

- `<future candidate>`
- `<future candidate>`
- `<future candidate>`

## Maintenance Rules

- Keep only active and near-future items here.
- Move deep design details into `docs/plans` or `docs/decisions`.
- Remove completed historical detail once it is no longer operationally useful.
- If a line item stays here for too long, either split it or archive it.
